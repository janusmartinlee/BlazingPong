namespace PongEngine;

public class PlayerVsBotMode : GameEngine
{
    public PlayerVsBotMode() :base()
    {
    }
    public override bool Update(InputState input)
    {
        // Handle the logic for bot
        return base.Update(input);
    }

    
}
